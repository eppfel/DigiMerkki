class CapacitiveSensor {
 
public:
 CapacitiveSensor(int x, int y);
 int capacitiveSensor(int x);
 void set_CS_AutocaL_Millis(int x);
};

CapacitiveSensor::CapacitiveSensor(int x, int y)
{
 
}

int CapacitiveSensor::capacitiveSensor(int x) {
 return 0;
}

void CapacitiveSensor::set_CS_AutocaL_Millis(int x) {
  return ;
}
